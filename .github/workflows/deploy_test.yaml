name: Update remote test server configs

on:
  push:
    branches: [master]

jobs:
  update_config:
    runs-on: ubuntu-latest
    steps:
      - name: fetch Nginx config
        uses: appleboy/ssh-action@v0.1.2
        with:
          host: ${{ secrets.TEST_SERVER_HOST }}
          username: root
          password: ${{ secrets.TEST_SERVER_SSH_KEY }}
          script: |
            cd /FedorenkaAvenue
            git pull origin master
            cp /FedorenkaAvenue/remote_test_server/nginx/nginx.conf /etc/nginx/
            cp /FedorenkaAvenue/remote_test_server/nginx/conf.d /etc/nginx/conf.d
            systemctl restart nginx

  deploy_main_site:
    runs-on: ubuntu-latest
    steps:
      - name: Update main site
        uses: appleboy/ssh-action@v0.1.2
        with:
          host: ${{ secrets.TEST_SERVER_HOST }}
          username: root
          password: ${{ secrets.TEST_SERVER_SSH_KEY }}
          script: |
            cd /FedorenkaAvenue
            git pull origin master
            cp /FedorenkaAvenue/site/index.html /var/www/html/fedorenka/
